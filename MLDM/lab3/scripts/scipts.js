var error_message = " ";
function isvalid(first,second,third){
    if (long > 0) {
        let st1 = first.split(' ');
        let st2 = second.split(' ');
        let st3 = rel;
        let valid = true;
        if(first.length > 0 && second.length > 0 &&  third.length > 0){
            if(st1.length+st2.length!=st3.length){
            error_message = " Длина строк должна совпадать с длинной отношения"
            }else {
    
        error_message = "Введите пары чисел!";
    }
}
}
}
function res(){
    document.getElementById('AB').innerHTML = "Отношение является функцией";
}